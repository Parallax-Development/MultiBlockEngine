rootProject.name = 'MultiBlockEngine'

include 'core'
include 'api'
include 'platform-bukkit'

['mbe-ui', 'mbe-wiring', 'mbe-crafting', 'mbe-electrics'].each { addon ->
    if (file("addons/${addon}").exists()) {
        include "addons:${addon}"
    }
}
